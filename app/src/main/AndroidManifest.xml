<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PawMart">
        <activity
            android:name=".ProductDetailActivity"
            android:exported="false" />
        <!-- AuthActivity does not have intent-filter, so it should NOT be exported -->
        <activity
            android:name=".AuthActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" /> <!-- Welcome screen → MUST BE EXPORTED (it has MAIN + LAUNCHER filter) -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
 MainActivity also has a launcher intent-filter → MUST be exported
             But since WelcomeActivity is the launcher, MainActivity should NOT have that intent-filter now.
             REMOVE the intent-filter from MainActivity!!
        -->
        <activity android:name=".PaymentActivity" />
        <activity android:name=".CheckoutActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC-8Fh_r600Wmmy34GZxd7G1GbLlcu43Xw" />
    </application>

</manifest>